# frontend/loader/ — tieir 加载 / 类加载器（并入前端职责）

对应设计 §3.3。tieir 反序列化 + 校验是 interp 解释执行的前置，天然归属前端。

- 反序列化优先 + 语法校验；可选签名校验（发布者公钥验签）。
- 校验失败 = 加载报错（安全底线）。`--strip` 去 span/文档段。
- 沿用 tieir-format §7。

当前为空目录，P0 起填充。